server {
    listen 80;
    server_name _;
    
    root /usr/share/nginx/html;
    index index.html;

    # Serve React app for all routes - let Ingress handle API routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
